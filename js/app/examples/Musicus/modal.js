define(function(require, exports, module) {

    // Modal for the Twitter example
    str = '<div class="socialbutton" id="gplus">'
    str += '<div class="signtext">Sign-in with Gmail</div>'
    str += '</div>'
    str += '<div class="socialbutton" id="twitter">'
    str += '<div class="signtext">Sign-in with Twitter</div>'
    str += '</div>'
    str += '<div class="socialbutton" id="facebook">'
    str += '<div class="signtext">Sign-in with Facebook</div>'
    str += '</div>'

    module.exports = str;
});